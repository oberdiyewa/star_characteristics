import 'package:flutter/material.dart';

class StarList extends StatelessWidget {
  const StarList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ýyldyzlar gollanmasy"),
      ),
      body: Text("data"),
    );
  }
}
